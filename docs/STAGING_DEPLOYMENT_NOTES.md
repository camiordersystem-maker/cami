# Staging Deployment Branch

This branch exists solely to give Vercel's GitHub integration a persistent
branch to build Preview deployments from. Do not merge this branch into
`main`; it should stay a thin, ever-current mirror of `main` plus this file.

See `docs/STAGING.md` for the required environment variables and setup.
